@model Convenience.Models.ViewModels.Shiire.ShiireKeysViewModel

@{
    ViewData["Title"] = "KeyInput";
}

<h1>仕入入力</h1>

<h4>キー入力</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShiireKeyInput">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label asp-for="ChumonId" class="control-label"></label>
                <select asp-for="ChumonId" asp-items="@Model.ChumonIdList" class="form-control">
                    <option value="">-- 選択してください --</option>
                </select>
                <span asp-validation-for="ChumonId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="ボタン" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
